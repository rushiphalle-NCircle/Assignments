<!-- Views/LevelPlanWindow.xaml -->
<Window x:Class="Asignment1.Commands.Views.LevelPlanWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Level ↔ Floor Plan" Height="450" Width="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Level ↔ Floor Plan Mapping"
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Data Grid -->
        <DataGrid Grid.Row="1"
                  x:Name="DataGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserResizeColumns="True"
                  ItemsSource="{Binding Items}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Level Id"
                                    Binding="{Binding LevelId}" Width="100"/>
                <DataGridTextColumn Header="Level"
                                    Binding="{Binding LevelName}" Width="*"/>
                <DataGridTextColumn Header="Elevation (ft)"
                                    Binding="{Binding ElevationFeet, StringFormat=F3}" Width="150"/>
                <DataGridTextColumn Header="Floor Plan"
                                    Binding="{Binding ViewName}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Close" Padding="10,4" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
