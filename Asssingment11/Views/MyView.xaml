<Window x:Class="inmbuild.Views.MyView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:m="clr-namespace:Assignment11.Model"
        Title="Level-wise Element Counts"
        Width="420"
        Height="560"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <HierarchicalDataTemplate DataType="{x:Type m:LevelNode}"
                                  ItemsSource="{Binding Children}">
            <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
        </HierarchicalDataTemplate>

        <DataTemplate DataType="{x:Type m:CategoryCountNode}">
            <TextBlock Text="{Binding DisplayName}"/>
        </DataTemplate>
    </Window.Resources>

    <DockPanel>
        <StackPanel DockPanel.Dock="Top"
                    Orientation="Horizontal"
                    Margin="8">
            <Button Content="Refresh"
                    Padding="10,4"
                    Command="{Binding RefreshCommand}"/>
        </StackPanel>

        <TreeView Margin="8"
                  ItemsSource="{Binding Levels}"/>
    </DockPanel>
</Window>
