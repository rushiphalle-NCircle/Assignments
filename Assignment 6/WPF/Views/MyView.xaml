<!-- File: Views/MyView.xaml -->
<Window x:Class="Assignment6.Commands.Views.MyView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800"
        Title="Select Floor &amp; Room">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <ComboBox Grid.Row="0"
                  ItemsSource="{Binding Floors}"
                  DisplayMemberPath="Name"
                  SelectedItem="{Binding SelectedFloor, Mode=TwoWay}"
                  MinWidth="220"/>

        <ComboBox Grid.Row="1" Margin="0,8,0,0"
                  ItemsSource="{Binding Rooms}"
                  DisplayMemberPath="Name"
                  SelectedItem="{Binding SelectedRoom, Mode=TwoWay}"
                  MinWidth="220"/>

        <Button Grid.Row="2" Margin="0,12,0,0" HorizontalAlignment="Center"
                Content="Show Walls"
                Command="{Binding ActivateCommand}"/>
    </Grid>
</Window>