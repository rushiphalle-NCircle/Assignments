<Window x:Class="FamilyLoader.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Family Loader" Height="180" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Folder selection -->
        <StackPanel Orientation="Horizontal">

            <TextBox Width="360" Margin="0,0,8,0" Text="{Binding FolderPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Browse..." Width="100" Command="{Binding BrowseCommand}" />
        </StackPanel>

        <!-- Spacer -->
        <Border Grid.Row="1"/>

        <!-- Load button -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left">
            <Button Content="Load Families" Width="120" Command="{Binding LoadCommand}" />
        </StackPanel>

        <!-- Spacer -->
        <Border Grid.Row="3"/>

        <!-- Status -->
        <TextBlock Grid.Row="4" Text="{Binding StatusText}" TextWrapping="Wrap" />

        <!-- Close -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Close" Width="80" Command="{Binding CloseCommand}" />
        </StackPanel>
    </Grid>
</Window>
